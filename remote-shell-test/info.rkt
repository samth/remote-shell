#lang info

(define collection "remote-shell")

(define deps '("base"
               "remote-shell-lib"
               "rackunit-lib"))

(define pkg-desc "tests for \"remote-shell\"")

(define pkg-authors '(mflatt))

(define version "1.10")

(define license
  '(Apache-2.0 OR MIT))
